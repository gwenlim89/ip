{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "java",
            "name": "Run Larper",
            "request": "launch",
            "mainClass": "larper.Larper",
            "cwd": "${workspaceFolder}"
        }
    ]
}
